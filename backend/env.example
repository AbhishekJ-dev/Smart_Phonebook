# Server configurations
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# JWT secret key for signing tokens
JWT_SECRET=smart_phonebook_ultra_secure_enterprise_key_2026
JWT_EXPIRES_IN=30d

# PostgreSQL Database credentials
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smart_phonebook

# Optional: Remote PostgreSQL connection string (useful for deployment)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/smart_phonebook
